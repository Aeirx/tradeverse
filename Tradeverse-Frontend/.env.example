# Tradeverse Frontend Environment Variables

# The base URL of the Tradeverse Backend API.
# All AI calls are proxied through the backend, so the frontend no longer
# needs (or should know) the AI service URL.
VITE_API_URL=http://localhost:8000
